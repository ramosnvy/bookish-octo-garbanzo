import AppLayout from "../components/AppLayout";

const DashboardPage = () => {
  return (
    <AppLayout title="Painel" subtitle="Visão geral da operação">
      <section className="card">
        <p>Nenhum dado disponível ainda.</p>
      </section>
    </AppLayout>
  );
};

export default DashboardPage;

